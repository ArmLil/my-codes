import styled from 'styled-components'

const Form = styled.form`
  margin-bottom: 1em;
  width: 12em;
  padding:1em;
  background-color: hsla(330, 83%, 96%, 0.90);
  box-shadow: inset 0.5px 0.5px 3px #000000;
`;

export default Form;
